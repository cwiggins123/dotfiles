# my super cool ratpoison config
escape C-s

exec rpws init 3 -k
#exec xcompmgr
#exec xwallpaper --zoom ~/Pictures/wallpapers/m.jpg
exec xrdb ~/.Xresources
exec dunst
exec tmux
# can get info from dunst pop ups
# exec sh ~/.config/dotfiles/termbar | dzen2 

unbind C-c
unbind c
# vim keys
unbind k
bind j focusdown
bind k focusup
bind h focusleft
bind l focusright
bind J exchangedown
bind K exchangeup
bind H exchangeleft
bind L exchangeright
bind C-k delete
# other stuff
bind C-c exec xterm -e tmux attach
bind c exec xterm -e tmux attach
bind b exec notify-send "$(acpi)"
unbind v
bind v exec notify-send "VOL: $(pamixer --get-volume)" 
bind M-l exec xlock
unbind C-s
unbind s
unbind C-S
unbind S
bind r restart
bind R quit
bind z split
bind C-z split
bind Z hsplit
bind C-Z hsplit
bind w exec firefox
bind C-w exec firefox

set border 0
set padding 0 0 0 0
set barborder 3
set barpadding 0 0 0 0
set fgcolor #d8caac
set bgcolor #323d43
set font IBM Plex Mono:size=18
set wingravity center
set transgravity center
set bargravity center

# special keys without escape key
definekey top XF86_AudioMute exec pamixer -t && notify-send "MUTE: $(pamixer --get-mute)"
definekey top XF86_AudioRaiseVolume exec pamixer -i 5 && notify-send "VOL: $(pamixer --get-volume)"
definekey top XF86_AudioLowerVolume exec pamixer -d 5 && notify-send "VOL: $(pamixer --get-volume)"
definekey top C-F9 exec xbacklight -dec 10
definekey top C-F10 exec xbacklight -inc 10
definekey top C-1 exec rpws 1
definekey top C-2 exec rpws 2
definekey top C-3 exec rpws 3
