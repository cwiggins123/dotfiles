# my super cool ratpoison config
exec xrdb ~/.Xresources
exec picom
exec dunst
# don't launch if tmux is running, avoids nested sessions
exec !(pidof tmux) && tmux
exec xmodmap ~/.xmodmap
exec xwallpaper --zoom ~/Pictures/d.jpg
# can get info from dunst pop ups
# exec sh ~/.config/dotfiles/termbar | dzen2 
# don't launch if terminal is already running
exec !(pidof urxvt) && urxvt -e tmux attach 

# change from xterm to urxvt
unbind C-c
unbind c
bind C-c exec urxvt -e tmux attach
bind c exec urxvt -e tmux attach
bind M-l exec xlock
unbind C-v
unbind v
bind C-v echo  "VOL: $(pamixer --get-volume)"
bind v exec notify-send  "VOL: $(pamixer --get-volume)"
bind b exec notify-send "$(acpi)"
#bind r restart
#bind C-q quit
bind w exec firefox
bind C-w exec firefox

set padding 0 0 0 0
set barborder 3
set fgcolor #b2b2b2
set bgcolor #080808
set font Hermit:size=18
set wingravity center
set transgravity center
set bargravity center

# special keys without escape key
definekey top XF86_AudioMute exec pamixer -t 
definekey top XF86_AudioRaiseVolume exec pamixer -i 5 && notify-send "VOL: $(pamixer --get-volume)"
definekey top XF86_AudioLowerVolume exec pamixer -d 5 && notify-send "VOL: $(pamixer --get-volume)"
definekey top C-F9 exec xbacklight -dec 10
definekey top C-F10 exec xbacklight -inc 10
